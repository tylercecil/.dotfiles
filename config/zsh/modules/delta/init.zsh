# See [delta](https://dandavison.github.io/delta/introduction.html)

# Set the pager to be less, with mouse input enabled
# (I was having trouble with this. Not sure why it's working in `bat` and not
# `delta` out of the box...)
export DELTA_PAGER='/usr/bin/less -RFX --mouse --wheel-lines=3'
